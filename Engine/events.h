#include "window.h"
/*
void pollEvents()
{
    sf::Event event;
    while (Window::getWindow().pollEvent(event))
    while (getpollEvent(event))
    {
        switch (event.type)
        {
        case sf::Event::Closed:
            window.close();
            break;
        case sf::Event::KeyReleased:
            switch (event.key.code)
            {
            case sf::Keyboard::Escape:
                window.close();
                break;
            default:
                break;
            }
        default:
            break;
        }
    }
}
*/